// Code generated by protoc-gen-cli dev. DO NOT EDIT.
// source: kitchensink/v1/relay.proto

package kitchensinkv1

import (
	bytes "bytes"
	context "context"
	json "encoding/json"
	errors "errors"
	fmt "fmt"
	io "io"
	maps "maps"
	os "os"
	slices "slices"
	strings "strings"

	table "github.com/jedib0t/go-pretty/v6/table"
	cobra "github.com/spf13/cobra"
	gjson "github.com/tidwall/gjson"
	term "golang.org/x/term"
	grpc "google.golang.org/grpc"
	protojson "google.golang.org/protobuf/encoding/protojson"
	proto "google.golang.org/protobuf/proto"
	yaml "sigs.k8s.io/yaml"
)

type cli_kitchensink_v1_relay_proto_viewField struct {
	Label string
	Path  string
}

type cli_kitchensink_v1_relay_proto_viewList struct {
	FullName string
	Label    string
	Path     string
	Fields   []cli_kitchensink_v1_relay_proto_viewField
}

// A view projects a message for display.
type cli_kitchensink_v1_relay_proto_view struct {
	Lists  []cli_kitchensink_v1_relay_proto_viewList
	Fields []cli_kitchensink_v1_relay_proto_viewField
}

// fullName's derived view with any configured fields applied.
func cli_kitchensink_v1_relay_proto_viewFor(fullName string, messageViews map[string]cli_kitchensink_v1_relay_proto_view, views map[string][]cli_kitchensink_v1_relay_proto_viewField) cli_kitchensink_v1_relay_proto_view {
	v := messageViews[fullName]
	if fields, ok := views[fullName]; ok {
		v.Fields = fields
	}
	if len(views) > 0 && len(v.Lists) > 0 {
		lists := slices.Clone(v.Lists)
		for i, l := range lists {
			if fields, ok := views[l.FullName]; ok {
				lists[i].Fields = fields
			}
		}
		v.Lists = lists
	}
	return v
}

func cli_kitchensink_v1_relay_proto_viewFields(message string, entries []string) []cli_kitchensink_v1_relay_proto_viewField {
	fields := make([]cli_kitchensink_v1_relay_proto_viewField, 0, len(entries))
	for _, entry := range entries {
		label, path, ok := strings.Cut(entry, ":")
		if !ok || label == "" || path == "" {
			panic(fmt.Sprintf("Options.Views[%q]: %q is not \"Label:path\"", message, entry))
		}
		fields = append(fields, cli_kitchensink_v1_relay_proto_viewField{Label: label, Path: path})
	}
	return fields
}

// Returns m as compact JSON. json.Compact makes protojson's deliberately
// unstable spacing reproducible.
func cli_kitchensink_v1_relay_proto_marshalJSON(m proto.Message) ([]byte, error) {
	raw, err := protojson.Marshal(m)
	if err != nil {
		return nil, err
	}
	var buf bytes.Buffer
	if err := json.Compact(&buf, raw); err != nil {
		return nil, err
	}
	return buf.Bytes(), nil
}

// Writes each record as a line of JSON.
func cli_kitchensink_v1_relay_proto_printJSON(indent bool) func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error {
	return func(w io.Writer, _ cli_kitchensink_v1_relay_proto_view, next func() ([]byte, error)) error {
		for {
			rec, err := next()
			if errors.Is(err, io.EOF) {
				return nil
			}
			if err != nil {
				return err
			}
			if indent {
				var buf bytes.Buffer
				if err := json.Indent(&buf, rec, "", "  "); err != nil {
					return err
				}
				rec = buf.Bytes()
			}
			if _, err := fmt.Fprintln(w, string(rec)); err != nil {
				return err
			}
		}
	}
}

// Writes each record as a YAML document, separated by "---".
func cli_kitchensink_v1_relay_proto_printYAML(w io.Writer, _ cli_kitchensink_v1_relay_proto_view, next func() ([]byte, error)) error {
	first := true
	for {
		rec, err := next()
		if errors.Is(err, io.EOF) {
			return nil
		}
		if err != nil {
			return err
		}
		if !first {
			if _, err := io.WriteString(w, "---\n"); err != nil {
				return err
			}
		}
		first = false
		doc, err := yaml.JSONToYAML(rec)
		if err != nil {
			return err
		}
		if _, err := w.Write(doc); err != nil {
			return err
		}
	}
}

// Renders a JSON value as one table cell: top-level entries stack, deeper
// nesting inlines.
func cli_kitchensink_v1_relay_proto_cell(v gjson.Result, sep string) string {
	switch {
	case v.IsArray():
		items := v.Array()
		parts := make([]string, len(items))
		for i, item := range items {
			parts[i] = cli_kitchensink_v1_relay_proto_cell(item, ",")
		}
		return strings.Join(parts, sep)
	case v.IsObject():
		var parts []string
		v.ForEach(func(k, val gjson.Result) bool {
			parts = append(parts, k.String()+"="+cli_kitchensink_v1_relay_proto_cell(val, ","))
			return true
		})
		return strings.Join(parts, sep)
	default:
		return v.String()
	}
}

// Renders each record as a table under its view.
func cli_kitchensink_v1_relay_proto_printTable(w io.Writer, v cli_kitchensink_v1_relay_proto_view, next func() ([]byte, error)) error {
	width := 0
	if f, ok := w.(*os.File); ok {
		if cols, _, err := term.GetSize(int(f.Fd())); err == nil {
			width = cols
		}
	}
	for i := 0; ; i++ {
		rec, err := next()
		if errors.Is(err, io.EOF) {
			return nil
		}
		if err != nil {
			return err
		}

		titled := len(v.Fields) > 0 || len(v.Lists) > 1
		first := i == 0
		section := func(title string, fields []cli_kitchensink_v1_relay_proto_viewField, rows []gjson.Result) error {
			t := table.NewWriter()
			if width > 0 {
				t.Style().Size.WidthMax = width
			}
			header := make(table.Row, len(fields))
			for j, f := range fields {
				header[j] = f.Label
			}
			t.AppendHeader(header)
			for _, src := range rows {
				row := make(table.Row, len(fields))
				for j, f := range fields {
					c := cli_kitchensink_v1_relay_proto_cell(src.Get(f.Path), "\n")
					if strings.Contains(c, "\n") {
						t.Style().Options.SeparateRows = true
					}
					row[j] = c
				}
				t.AppendRow(row)
			}
			out := ""
			if !first {
				out += "\n"
			}
			first = false
			if title != "" {
				out += title + "\n"
			}
			out += strings.TrimRight(t.Render(), "\n") + "\n"
			_, err := io.WriteString(w, out)
			return err
		}

		if len(v.Fields) > 0 {
			if err := section("", v.Fields, []gjson.Result{gjson.ParseBytes(rec)}); err != nil {
				return err
			}
		}
		for _, list := range v.Lists {
			rows := gjson.GetBytes(rec, list.Path).Array()
			if len(rows) == 0 {
				continue
			}
			title := ""
			if titled {
				title = strings.ToUpper(list.Label)
			}
			if err := section(title, list.Fields, rows); err != nil {
				return err
			}
		}
	}
}

// The built-in -o formats.
func cli_kitchensink_v1_relay_proto_printers() map[string]func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error {
	return map[string]func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error{
		"json":        cli_kitchensink_v1_relay_proto_printJSON(false),
		"json-pretty": cli_kitchensink_v1_relay_proto_printJSON(true),
		"table":       cli_kitchensink_v1_relay_proto_printTable,
		"yaml":        cli_kitchensink_v1_relay_proto_printYAML,
	}
}

func cli_kitchensink_v1_relay_proto_checkDefaultOutput(printers map[string]func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error, defaultOutput string) {
	if defaultOutput == "" {
		return
	}
	if _, ok := printers[defaultOutput]; !ok {
		panic(fmt.Sprintf("Options.DefaultOutput: unknown format %q; use one of: %s",
			defaultOutput, strings.Join(slices.Sorted(maps.Keys(printers)), ", ")))
	}
}

// Resolves -o to its print function. Empty -o means the configured default,
// or JSON: pretty on a terminal, compact otherwise.
func cli_kitchensink_v1_relay_proto_outputPrinter(cmd *cobra.Command, printers map[string]func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error, defaultOutput string) (func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error, error) {
	format, _ := cmd.Flags().GetString("output")
	if format == "" {
		format = defaultOutput
	}
	if format == "" {
		pretty := false
		if f, ok := cmd.OutOrStdout().(*os.File); ok {
			pretty = term.IsTerminal(int(f.Fd()))
		}
		return cli_kitchensink_v1_relay_proto_printJSON(pretty), nil
	}
	p, ok := printers[format]
	if !ok {
		return nil, fmt.Errorf("unknown output format %q; use one of: %s",
			format, strings.Join(slices.Sorted(maps.Keys(printers)), ", "))
	}
	return p, nil
}

// The -o usage text.
func cli_kitchensink_v1_relay_proto_outputHelp(printers map[string]func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error, defaultOutput string) string {
	help := "Output format: " + strings.Join(slices.Sorted(maps.Keys(printers)), ", ") + ".\n"
	if defaultOutput != "" {
		return help + "Default: " + defaultOutput + "."
	}
	return help + "Default: pretty JSON on a terminal, compact when piped."
}

// Decodes JSON requests from in, one after another, and sends each.
func cli_kitchensink_v1_relay_proto_pumpRequests(ctx context.Context, in io.Reader, errW io.Writer, send func(n int, raw json.RawMessage) error) error {
	if f, ok := in.(*os.File); ok && term.IsTerminal(int(f.Fd())) {
		fmt.Fprintln(errW,
			"reading JSON requests from the terminal, one after another; Ctrl-D to finish")
	}
	dec := json.NewDecoder(in)
	for n := 1; ; n++ {
		if err := ctx.Err(); err != nil {
			return err
		}
		var raw json.RawMessage
		if err := dec.Decode(&raw); err != nil {
			if errors.Is(err, io.EOF) {
				return nil
			}
			return fmt.Errorf("stdin request %d: %w", n, err)
		}
		if err := send(n, raw); err != nil {
			return err
		}
	}
}

// Renders responses while a goroutine pumps stdin requests over the stream's
// ctx. The pump is never joined (it may be blocked on stdin); a pump failure
// cancels the stream, so its error wins over the canceled render.
func cli_kitchensink_v1_relay_proto_runBidi(ctx context.Context, cancel context.CancelFunc, cmd *cobra.Command, print func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error, v cli_kitchensink_v1_relay_proto_view, send func(int, json.RawMessage) error, closeSend func() error, next func() ([]byte, error)) error {
	sendErr := make(chan error, 1)
	go func() {
		err := cli_kitchensink_v1_relay_proto_pumpRequests(ctx, cmd.InOrStdin(), cmd.ErrOrStderr(), send)
		if closeErr := closeSend(); err == nil {
			err = closeErr
		}
		sendErr <- err
		if err != nil && !errors.Is(err, io.EOF) && !errors.Is(err, context.Canceled) {
			cancel()
		}
	}()

	renderErr := print(cmd.OutOrStdout(), v, next)
	cancel()
	select {
	case err := <-sendErr:
		if err != nil && !errors.Is(err, io.EOF) && !errors.Is(err, context.Canceled) {
			return err
		}
	default:
	}
	return renderErr
}

// RelayServiceOptions configures the RelayService commands.
type RelayServiceOptions struct {
	// Printers adds -o formats: print pulls JSON records from next until
	// io.EOF; a nil func removes the named format.
	Printers map[string]func(w io.Writer, next func() ([]byte, error)) error
	// DefaultOutput is the format used when -o is empty.
	DefaultOutput string
	// Views maps a message's full proto name to its "Label:path" fields
	// (gjson paths), replacing the message's derived fields wherever it
	// renders; a malformed entry panics, a name never displayed is inert.
	Views map[string][]string
}

// NewRelayServiceCommand returns the RelayService command with one
// subcommand per RPC. Later opts override earlier ones per entry.
func NewRelayServiceCommand(conn grpc.ClientConnInterface, opts ...RelayServiceOptions) *cobra.Command {
	client := NewRelayServiceClient(conn)

	printers := cli_kitchensink_v1_relay_proto_printers()
	defaultOutput := ""
	views := map[string][]cli_kitchensink_v1_relay_proto_viewField{}
	for _, o := range opts {
		for name, fn := range o.Printers {
			if fn == nil {
				delete(printers, name)
				continue
			}
			printers[name] = func(w io.Writer, _ cli_kitchensink_v1_relay_proto_view, next func() ([]byte, error)) error {
				return fn(w, next)
			}
		}
		if o.DefaultOutput != "" {
			defaultOutput = o.DefaultOutput
		}
		for message, entries := range o.Views {
			views[message] = cli_kitchensink_v1_relay_proto_viewFields(message, entries)
		}
	}
	cli_kitchensink_v1_relay_proto_checkDefaultOutput(printers, defaultOutput)

	// The derived views; Options.Views entries override them.
	messageViews := map[string]cli_kitchensink_v1_relay_proto_view{
		"kitchensink.v1.ChatNote": {Fields: []cli_kitchensink_v1_relay_proto_viewField{
			{Label: "text", Path: "text"},
		}},
	}

	viewFor := func(fullName string) cli_kitchensink_v1_relay_proto_view {
		return cli_kitchensink_v1_relay_proto_viewFor(fullName, messageViews, views)
	}
	outputPrinter := func(cmd *cobra.Command) (func(io.Writer, cli_kitchensink_v1_relay_proto_view, func() ([]byte, error)) error, error) {
		return cli_kitchensink_v1_relay_proto_outputPrinter(cmd, printers, defaultOutput)
	}

	cmd := &cobra.Command{
		Use: "relay",
	}
	cmd.PersistentFlags().StringP("output", "o", "", cli_kitchensink_v1_relay_proto_outputHelp(printers, defaultOutput))
	cmd.AddCommand(func() *cobra.Command {
		sub := &cobra.Command{
			Use:  "chat",
			Args: cobra.NoArgs,
			RunE: func(cmd *cobra.Command, _ []string) error {
				print, err := outputPrinter(cmd)
				if err != nil {
					return err
				}
				ctx, cancel := context.WithCancel(cmd.Context())
				defer cancel()
				stream, err := client.Chat(ctx)
				if err != nil {
					return err
				}
				send := func(n int, raw json.RawMessage) error {
					req := &ChatNote{}
					if err := protojson.Unmarshal(raw, req); err != nil {
						return fmt.Errorf("stdin request %d: %w", n, err)
					}
					return stream.Send(req)
				}
				next := func() ([]byte, error) {
					m, err := stream.Recv()
					if err != nil {
						return nil, err
					}
					return cli_kitchensink_v1_relay_proto_marshalJSON(m)
				}
				return cli_kitchensink_v1_relay_proto_runBidi(ctx, cancel, cmd, print, viewFor("kitchensink.v1.ChatNote"), send, stream.CloseSend, next)
			},
		}
		return sub
	}())
	return cmd
}
