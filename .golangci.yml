version: "2"

run:
  go: "1.26"
  relative-path-mode: cfg

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines

linters:
  default: standard
  enable:
    - errname
  exclusions:
    generated: strict
