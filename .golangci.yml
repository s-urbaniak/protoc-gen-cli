version: "2"

run:
  concurrency: 1
  go: "1.26"
  relative-path-mode: cfg
  build-tags:
    - codeanalysis

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines

linters:
  default: standard
  enable:
    - errname
  exclusions:
    generated: strict
